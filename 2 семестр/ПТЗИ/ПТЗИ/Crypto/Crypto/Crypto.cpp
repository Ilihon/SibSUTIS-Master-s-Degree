// Crypto.cpp: определяет точку входа для консольного приложения.
//

#include "stdafx.h"
#include "Crypto.h"


int main()
{
	//ll A[3] = {};
	//ll key,b,s;
	//char o;
	string st="";
	srand(time(0));
	/*cin >>q;
	for(;q!=666;){
	cout <<endl<< simp(q)<<endl;
	cin >>q;
	}*/
	//cout <<Fpow(2,39547,89051)<<endl;
	/*Evklld(324,143,A);
	cout << A[0] << " " << A[1] << " " << A[2] << endl;*/
	//DH();
	//cout<<LBStep(103,107,109)<<endl;
	//cout << Fpow(103, LBStep(103, 107, 109), 109) << endl;
	/*RSA(54, key, b, s);
	cout << b << " " << key << " " << s << endl;
	cout << Fpow(b, key, s) << endl;*/
	/*cout << "Input number of coding: \n 1.RSA \n 2.Vernam \n 3.ElGamal" << endl;
	cin >> sw;
	FileEnc("test.bmp");
	FileDec(); */
	/*s = 2;
	Sha(s);*/
	/*VC("test.bmp");
	VE("test.bmp");*/
	//CompHash("1.txt",st);
	/*ll r=0, x=0, y=0, g=0, p=0;
	EGDS(138,r,s,x,y,g,p);
	cout << " r " << r << " s " << s << " x " << x << " y " << y << " g " << g << " p " << p << endl;
	cout << " g^h " << ((Fpow(y, r, p)*Fpow(r, s, p)) % p )<< endl;
	cout << " t g^h " << Fpow(g, 138, p) << endl;  */
	/*cout << " Input number of digital signature" << endl;
	cin >> sw;
	DigSig("1.txt");
	chkDigSig("1.txt"); */
	/*ll p = 0, q = 0, s = 0, r = 0, a = 0, k = 0, x = 0, y = 0;
	Ghost(138, p, q, s, r, a, k, x, y);
	ChkG(138, p, q, s, r, a, y);*/
	//Poker();
	/*Fill(D);
	pD(D);
	shuffle(D);
	getchar();
	pD(D);*/
	//Emoney();
	//cout << Fpow(1091258852, 207351011, 1153079897) << endl;
	Gamilton();
    return 0;
}

